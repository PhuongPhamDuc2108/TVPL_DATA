# Copy file này thành .env rồi điền giá trị thật. File .env không lên git.
#   cp .env.example .env        (Git Bash)
#   copy .env.example .env      (cmd)

# Key Gemini - lấy tại https://aistudio.google.com/apikey
# Free tier: 20 request/ngày/model, nên gemini_ocr.py tự tụt xuống model kế tiếp.
GEMINI_API_KEY=

# Postgres, dạng postgresql://user:mat_khau@host:cong/ten_db
DATABASE_URL=

# (tuỳ chọn) ép model cho trich_dan_llm.py / gemini_ocr.py
# GEMINI_MODEL=gemma-4-31b-it
# GEMINI_OCR_MODEL=
